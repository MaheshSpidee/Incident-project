{
  "name": "incident-alert-management-system",
  "private": true,
  "version": "1.0.0",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "dev": "concurrently \"npm run dev -w backend\" \"npm run dev -w front-end\"",
    "build": "npm run build -w backend && npm run build -w front-end",
    "typecheck": "npm run typecheck -w backend && npm run typecheck -w front-end",
    "test": "npm run test -w backend && npm run test -w front-end",
    "lint": "npm run typecheck",
    "db:migrate": "npm run db:migrate -w backend",
    "db:seed": "npm run db:seed -w backend",
    "worker": "npm run worker -w backend"
  },
  "workspaces": [
    "backend",
    "front-end"
  ],
  "devDependencies": {
    "concurrently": "9.1.2"
  }
}
